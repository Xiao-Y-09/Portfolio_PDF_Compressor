# PDF 压缩 SaaS 后端依赖（Python 3.11+）
# 依赖清单严格对应《PDF压缩SaaS构建手册.md》Phase 0 列出的范围。
# 手册后续 Phase 明确列出的依赖（如 Phase 7 的 fonttools）在对应 Phase 加入。

fastapi
uvicorn[standard]
pydantic>=2
pydantic-settings
pymupdf
pillow
celery
redis
boto3
python-multipart
pyyaml  # config.yaml 加载（代码直接 import yaml，必须显式声明，不依赖传递依赖）
fonttools  # Phase 7 字体子集化（手册 Phase 7 Prompt 列出的依赖）

# 测试
pytest
pytest-cov  # 覆盖率报告（Tier 系统步骤 4 用户指定，2026-07-04）
httpx
