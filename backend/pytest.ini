[pytest]
testpaths = tests
; Windows 上系统级 %TEMP%\pytest-of-<user> 目录可能存在 ACL 损坏导致
; tmp_path fixture 报 PermissionError（本机已实测）。统一使用项目内
; 临时目录（已在 .gitignore），开发者与 CI 无需手动传参。
addopts = --basetemp=.pytest_tmp
